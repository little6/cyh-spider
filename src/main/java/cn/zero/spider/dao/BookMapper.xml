<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.zero.spider.dao.BookMapper">
    <insert id="save" parameterType="cn.zero.spider.pojo.Book">
        insert ignore into book
        (bookUrl, author, title, updateTime, intro, latestChapterTitle, latestChaterUrl, titlePageUrl, sourceUrl, chapterPage)
        VALUES
        (#{bookUrl},#{author},#{title},#{updateTime},#{intro},#{latestChapterTitle},#{latestChapterUrl},#{titlePageUrl}
        ,#{sourceUrl},#{chapterPage})
    </insert>
    <select id="getById" resultType="cn.zero.spider.pojo.Book" parameterType="String">
        select * from book where bookUrl=#{value}
    </select>

</mapper>